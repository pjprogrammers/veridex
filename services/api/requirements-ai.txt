# Heavy AI dependencies - kept separate for maintainability.
# These require Python 3.11 (paddlex/insightface/onnxruntime do not
# yet support newer Python versions portably). Install in the AI
# service Docker images, NOT in the core API image.
#
# Pinned to the VERIDEX — AI / CV layer specification versions.
paddleocr==3.7.0
paddlepaddle==3.3.1
insightface==1.0.1
onnxruntime==1.29.0
