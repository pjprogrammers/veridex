# Allow lifecycle scripts only for packages that need them at build time.
# (Global config restricts to opencode-ai; project override adds build deps.)
allow-scripts=@tailwindcss/oxide,next,eslint,sharp,unrs-resolver